1.-
    Acceder a un archivo: admin_employees.php que tenga un botón en el que
    cuando cliquemos, se nos reinicie la base de datos employees.


2.-
    Crear una BD (my_employees) nueva con una sola tabla (employees) para
    gestionar la subida de archivos a la BD (id, nombre, foto).
    HINT:   La foto la guardamos en una carpeta, y en la tabla se almacena
            como la ruta.

    --> my_employees.php?id=id_employee <--
    Si no pasamos el id_employee en el GET, listamos todos (nombre y foto).
    Si clicamos en uno o paramos el id_employee por la url, le enseñamos un
    formulario en el que se muestra el nombre y un campo para editar la foto 
    del empleado (añadirle imagen por defecto) y un botón para volver. 